<% @devices.each do |device| %>
dhcp-host=<%= device.macaddress.downcase %>,<%= device.user.lastname.downcase %>-<%= device.user.firstname.downcase %>-<%= device.group.downcase %>-<%= device.manufacturer.downcase %>,<%= device.user.iprange.ipbase %><%= device.ip %>,12h
<% end %>