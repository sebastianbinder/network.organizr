<% @devices.each do |device| %>
dhcp-host=<% unless device.macaddresswifi.blank? %><%= device.macaddresswifi.downcase %>,<% end %><% unless device.macaddresslan.blank? %><%= device.macaddresslan.downcase %>,<% end %><%= device.user.lastname.downcase %>-<%= device.user.firstname.downcase %>-<%= device.group.downcase %>-<%= device.manufacturer.downcase %>,<%= device.user.iprange.ipbase %><%= device.ip %>,12h
<% end %>