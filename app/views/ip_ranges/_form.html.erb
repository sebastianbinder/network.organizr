<%= form_for(@ip_range) do |f| %>
  <% if @ip_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip_range.errors.count, "error") %> prohibited this ip_range from being saved:</h2>

      <ul>
      <% @ip_range.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ipbase %><br>
    <%= f.text_field :ipbase %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
